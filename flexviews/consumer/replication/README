FlexSBR is an extension of FlexCDC which ignores RBR and runs all SBR it encounters.

This simple utility can be used to implement statement based replication between multiple masters and a single slave.
